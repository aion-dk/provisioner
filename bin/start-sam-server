#!/bin/sh

sam local start-api \
  --port 3009 \
  --host '0.0.0.0' \
  --container-host 'host.docker.internal' \
  --env-vars local-env-linux.json \
  --warm-containers LAZY \
  --skip-pull-image --debug
